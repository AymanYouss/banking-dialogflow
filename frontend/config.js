// Fill these in after you create your Dialogflow CX agent (see SETUP_DIALOGFLOW_CX.md).
// Until PROJECT_ID/AGENT_ID are set, the page runs in LOCAL DEMO mode using the
// /api/simulate endpoint so you can still see the live dashboard updates.
window.CX_CONFIG = {
  PROJECT_ID: "YOUR_GCP_PROJECT_ID",
  AGENT_ID: "YOUR_DIALOGFLOW_CX_AGENT_ID",
  LOCATION: "global", // e.g. "global" or "us-central1"
  LANGUAGE: "en",
};
